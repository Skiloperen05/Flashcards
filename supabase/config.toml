project_id = "haugnes-flashcards"

[functions.timeedit]
verify_jwt = false

[functions.create-stripe-checkout]
verify_jwt = false

[functions.stripe-webhook]
verify_jwt = false

[functions.drive-proxy]
# The function validates the bearer token itself so it can return CORS-aware
# errors and use the caller's JWT for RLS-enforced file lookup.
verify_jwt = false
